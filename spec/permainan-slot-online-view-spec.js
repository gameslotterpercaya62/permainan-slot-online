'use babel';

import PermainanSlotOnlineView from '../lib/permainan-slot-online-view';

describe('PermainanSlotOnlineView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
